:root {
    --wm-rel: rgb(233, 203, 79);          /* the game's gold: badge level + gift gain fill */
    --wm-bar-track: rgba(0, 0, 0, 0.33);  /* empty bar track (matches native bars) */
    --wm-bar-border: rgba(0, 0, 0, 0.5);  /* 1px bar outline (matches native bars) */
}
